# Edit An Existing Flato Project

Use Flato Design MCP to edit an existing Flato project URL supplied by the user.

First call `flato_use_project`, wait for `canWrite=true`, then call
`flato_get_design_context` before making any edits.

Do not guess page or block ids.
